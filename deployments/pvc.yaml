apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: all-in-one-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Uncomment and modify based on your cluster's storage class
  # storageClassName: standard
